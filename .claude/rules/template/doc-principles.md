<!-- [Template] research-project-template 由来。プロジェクト固有の記述は .claude/CLAUDE.md に書くこと -->

## ドキュメント原則

### What vs How

- **README（What）**: このプロジェクトは何か、何ができるか、何が含まれているか
- **CLAUDE.md（How）**: どうやって作業するか、どういうルールで進めるか

### 変更時の注意

- 手順やルールの追加・変更は CLAUDE.md に記載
- プロジェクト概要やセットアップ手順は README に記載
- 同じ情報を両方に書かない（Single Source of Truth）

### ★ アカウント層（`~/.claude/`）に何を置くか

**`~/.claude/`（アカウント層）には個人の好み（言語・出力スタイル等）のみを置きます。**
**プロジェクトのワークフローに影響するルールは置きません。**
ワークフロールールはリポジトリ内（`.claude/rules/template/` と `.claude/CLAUDE.md`）に置き、
`/template-sync` で配布します。

| 内容 | 置き場所 |
|---|---|
| 応答言語、出力スタイル、個人的なエイリアス | `~/.claude/` |
| ワークフロー、Issue 運用、レビュー手順、コード規約 | リポジトリ内（`.claude/`） |

理由:

- **共同研究者の環境に存在しない。** clone しただけではルールが無く、ワークフローが崩壊する
- **devcontainer にマウントされない。** ホストの `~/.claude` はコンテナ内から見えない
- **外部インポート `@~/...` は罠。** 承認ダイアログを一度拒否すると恒久的に無効化され、
  以降ルールが黙って読み込まれなくなる
- **更新の伝播コスト。** アカウント層は全マシンで手動更新が必要。リポジトリ内なら sync 1回

AGENTS.md 階層標準の nearest-file-wins は、**最も近いファイル＝リポジトリ内の配置が勝つ**
という優先則であり、リポジトリ内配置を正とする根拠そのものです。
