# Issue #100 自動判断ログ

## メタ情報
- 判断日時: 2026-07-31
- 自動処理: /task-run #99 Step 1.5（auto-reviewer 代理判断）
- 判断対象: `.spec/issues/100-sync-reflux-account.md`（v2）
- 必読コンテキスト: `.spec/core-rules.md` / `.spec/invariants.md` / `.spec/known-issues.md` — **3ファイルとも既定節あり（必読コンテキストあり）**。ただし3ファイルとも「プロジェクト固有」節は未記入のため、既定のみで判断した（本テンプレート自体が対象プロジェクトであるため実質的影響は小さい）

## 1. review-spec 指摘の照合表（v1 指摘 → v2 反映状況）

| # | 指摘（観点） | v2 での対応箇所 | 判定 |
|---|---|---|---|
| F1 | rm-rf→cp 非アトミック（品質・重大） | D1-a: 一時領域展開→mv 入れ替え、失敗時は無変更＋非0 exit | ✅ 解消（残課題 R1 あり、下記） |
| F2 | sync がローカル改変＝還流候補を無警告破壊（品質・重大） | D1-b: MANIFEST.sha256 照合→`template.bak-<日付>/` 退避→サマリ表示。MANIFEST 無しは全件退避 | ✅ 解消 |
| M1 | D3 に検証 V が無い（品質・中） | V9（doc-principles.md のガイド節存在確認）を追加 | ✅ 解消 |
| — | V4-V7 の PASS 条件曖昧（品質） | V4: exit 非0＋ファイル名 / V5: unified diff 生成 / V6: 3分類の実行 / V7: canary ルール — いずれも機械判定可能 | ✅ 解消 |
| — | K1 出典未記載（品質） | §0 に公式ドキュメント URL・引用文・取得日（2026-07-31）・再確認の旨を記載。KI-D13 の要求と整合 | ✅ 解消 |
| — | V8 冪等・フィクスチャ定義欠落（品質） | V8: 2回目の変更 0 ファイル / V1: フィクスチャ3点（固有節1行・ローカルファイル1個・KI-001 1件）を明記 | ✅ 解消 |
| — | D2「auto-reviewer への指示」節が固有節の後ろで更新非伝播（実現性） | D2: 同節を既定節末尾へ移動する構造変更を明記 | ✅ 解消（残課題 R2 あり、下記） |
| — | D5 #91 フラット世代の孤児二重読み込み（実現性） | D5-a: MANIFEST 突合による3分類（削除/退避/保持） | ✅ 解消（残課題 R3 あり、下記） |
| — | 修正対象既存ファイルの列挙欠落（設計） | D1-c: 5系統のファイルと修正内容の表。rules 11ファイルは実ディレクトリと一致することを確認済み | ✅ 解消 |
| — | rm -rf とディレクトリ移動の別 PR 化が破壊的（設計） | D1-c「実装順序の制約」＋§4 で #74 を「原子的に1 PR」と明記 | ✅ 解消 |
| — | D3 と #74 の goal 書き換え疑義（設計: KI-D14 非該当と判定） | D3 末尾: #74 への決定コメント・タイトル手当て・#102 切り出し・スコープ外宣言 | ✅ 解消（条件付き。D3 の判定参照） |
| — | issue-finish の in-progress 不整合（設計） | D1-c: issue-finish SKILL.md の修正対象化。実ファイル L16/L138-141 に `in-progress` 削除処理が実在し、labels.md「使わない」と矛盾していることを確認済み | ✅ 解消 |

**未解消の指摘: なし。** ただし新規に検出した残課題 R1〜R3（下記「要確認フラグ」）を実装時に扱うこと。

## 2. D1〜D5 の判定

### D1: rules/template/ 隔離＋アトミック sync＋改変退避

| 項目 | 内容 |
|------|------|
| 判断 | ✅ 許可 |
| 理由 | sync 対象の機械的確定は F2（固有記述の破壊）への根本対処であり、対症療法ではない（R-D06 整合）。失敗時に非0 exit で止まる設計は R-D01/KI-D03（前提が崩れたら raise）と整合。同一 PR 制約により移行期間の破壊的動作を排除 |
| 参照 | core-rules R-D01/R-D06、known-issues KI-D03、template-sync/SKILL.md L36（`rm -rf .claude/rules/` の実在確認）、rules/ 実ファイル11個 |
| 自信度 | 90% |

### D2: .spec 既定節のマーカー差し替え

| 項目 | 内容 |
|------|------|
| 判断 | ⚠️ 警告付き許可 |
| 理由 | マーカーの一意性は実現性レビューで確認済み、マーカー不在時の非0 exit は「静かな腐敗」を防ぐ正しい設計。ただし既存派生プロジェクトでは「auto-reviewer への指示」節の移動により**旧位置の同名節が残存して重複する**（残課題 R2）。実3ファイルで同節が固有節の後ろ（L91/L83/L183）にあることを確認済み |
| 参照 | .spec 3ファイルの実構造、レビュー指摘 F3 |
| 自信度 | 80% |

### D3: アカウント単位化の不採用

| 項目 | 内容 |
|------|------|
| 判断 | ⚠️ 警告付き許可 |
| 理由 | goal 書き換えチェック（最優先）を実施した。結論は設計レビューと同じく **KI-D14 非該当**（根拠は下記「3. goal 書き換えの検査」）。技術判断そのものは妥当: devcontainer に `~/.claude` が無い事実、外部インポート拒否の恒久無効化、nearest-file-wins がリポジトリ内配置を正とする論理は、#74 本文の論点（devcontainer 整合・共同研究者・業界標準）への直接回答になっている |
| 条件 | (1) #74 への決定コメントに**旧タイトルを引用**し、完了条件4（→#102）・条件5（スコープ外）の扱いを明記すること（黙ってタイトルだけ変えない） (2) task #99 成功条件(3) の解釈（下記）を実装 issue に引き継ぐこと |
| 参照 | known-issues KI-D14、GitHub #74 本文（完了条件・「提案（要相談）」の実文言を確認）、#102 の実在確認（「#74から切り出し」タイトル） |
| 自信度 | 85% |

### D4: contribute の半自動化＋issue-finish 契機

| 項目 | 内容 |
|------|------|
| 判断 | ✅ 許可 |
| 理由 | 還流 0 件の原因仮説（コスト・基準・契機）それぞれに機構で対処しており対症療法ではない。auto モードで質問せず完了報告コメントに記録する設計は、silent に握りつぶさない（記録が残る）ため R-D01 と整合。退避ディレクトリを検出対象に含める点で D1-b と閉ループになっている |
| 参照 | core-rules R-D01、issue-finish/SKILL.md（既存フローの確認） |
| 自信度 | 85% |

### D5: 旧構造からの移行

| 項目 | 内容 |
|------|------|
| 判断 | ⚠️ 警告付き許可 |
| 理由 | 二重読み込み（矛盾ルールの同時注入）への対処は必要。3分類のうち「退避」「保持」は非破壊。ただし「テンプレート旧版と内容一致→削除」は**旧版ハッシュの入手手段が仕様に無い**（残課題 R3）。543行世代の自動削除をせず user-action 起票とする設計は「固有記述の自動削除禁止」と整合 |
| 条件 | 実装では**ハッシュ一致を証明できないファイルを削除しない**こと。判定不能時の既定は「退避」（安全側） |
| 参照 | D1-b、Fallback ホワイトリスト §3 |
| 自信度 | 75% |

## 3. goal 書き換えの検査（最優先項目）

**親 task #99 / epic の goal 書き換え: 含まれない。**

- task #99 の goal「sync/還流の仕組みをテンプレート側で完成させる」と成功条件(1)(2)(3) は仕様内で一切改変されていない
- 成功条件(3)「~/.claude/ 階層化の設計が確定し実装済み」に対し、D3 は「不採用の決定＋使い分けガイドの実装（V9 で検証）」で応える。これは**goal の解釈の明確化**（「設計が確定」＝採否の決定と根拠の文書化を含む）であり、範囲・成功条件の変更ではない → ⚠️ 警告付き許可の枠。**この解釈を本ログに明記する**
- issue #74 について: 「アカウント単位化」は #74 本文で「提案（要相談）」と明記された方針1であり、D3 はその相談への回答。完了条件4は削除ではなく #102 への切り出し（実在確認済み）、条件5（PushVoice 分析）は方針1不採用により手段として不要になったもので、**結果に合わせた post-hoc の書き換えではない**。決定コメントで記録を残す前提（上記 D3 の条件）で許容
- KI-D14 の「正しい対処」（変更するなら記録を残して明示的に閉じる）に沿った手当てが仕様に含まれている

## 4. Fallback ホワイトリストと R-D01 の整合

| 仕様のホワイトリスト項目 | 判定 |
|---|---|
| テンプレート取得失敗 → 無変更終了（非0 exit＋エラーメッセージ必須） | ✅ 整合。「黙って既定値を返す」fallback ではなく、前提崩壊時に**明示的に失敗する**挙動であり、R-D01/KI-D03 の要求そのもの |
| .spec マーカー不在 → スキップ（非0 exit＋サマリ明示必須） | ✅ 整合。同上。無言 exit 0 の明示的禁止（F3 対応）は silent-wrong の防止と一致 |
| 禁止リスト（固有記述の自動削除・確認なし CLAUDE.md 書き換え・退避なし上書き） | ✅ R-D01 の例外枠（設定既定値・キャッシュ・UI プレースホルダ）を拡大していない |

## 5. 自信度と結論

**総合自信度: 82%（自動判断を実行、要確認フラグ付き）**

上げる要因: 全指摘に対応箇所が明示され、仕様の事実主張（rm -rf の実在、in-progress 不整合、rules 11ファイル、#74/#102/#99 の実在と内容、.spec 節構造）を**実ファイル・実 issue で直接照合して全件一致**。出典は一次資料付き（KI-D13 充足）。
下げる要因: 残課題 R1〜R3 が実装時対応に委ねられる。`.spec/` プロジェクト固有節が未記入。

## 要確認フラグ（自信度 50-79% 相当の残課題。実装 issue #74/#80 で対応すること）

- [ ] **R1（D1-a、実装時）**: 例示スニペットは前回失敗の残骸 `.template.new/` が存在すると `cp -r` がその**中に**コピーして入れ子になる。実装では冒頭で `rm -rf "$RULES_DIR/.template.new"` を行うこと。また `rm -rf template` 成功後 `mv` 失敗の瞬間は template/ が不在になる（.template.new から復旧可能なので許容だが、エラーメッセージに復旧手順を含めること）
- [ ] **R2（D2、実装時）**: 既存派生プロジェクトに sync すると「auto-reviewer への指示」節が既定節末尾（新）と固有節の後ろ（旧・stale）に**重複**する。sync-spec-defaults.sh で旧位置の同名節を検出・除去するか、少なくとも終了サマリで警告すること
- [ ] **R3（D5-a、実装時）**: 「テンプレート旧版と内容一致→削除」に必要な旧版ハッシュの入手手段が未定義。ハッシュ一致を証明できない場合の既定は「削除」ではなく「退避」とすること
- [ ] `.spec/` 3ファイルの「プロジェクト固有」節が未記入のまま判断した（既定は完備）

## 参照したコンテキスト

- `.spec/core-rules.md`（R-D01, R-D06）/ `.spec/invariants.md`（抵触なし）/ `.spec/known-issues.md`（KI-D03, KI-D13, KI-D14）
- `.claude/rules/`（11ファイル実在確認）、`.claude/rules/labels.md`（in-progress 廃止）
- `.claude/skills/template-sync/SKILL.md` L36 / `.claude/skills/issue-finish/SKILL.md` L16, L138-141
- GitHub Issue #74（本文・タイトル・完了条件）、#102（実在）、#99（タイトル）
