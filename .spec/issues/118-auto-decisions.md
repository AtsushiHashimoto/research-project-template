# Issue #118 自動判断ログ

## メタ情報
- 判断日時: 2026-08-02
- 自動処理: /task-run（task #115 配下）
- 判断対象: .spec/issues/118-label-consistency.md
- 必読コンテキスト: .spec/core-rules.md / invariants.md / known-issues.md（既定節あり。
  **プロジェクト固有節は3ファイルとも未記入**のため、既定のみで判断した）

## 判断一覧

### 1. `review-integrity` ラベルの新設（labels.md に「報告」節を追加）

| 項目 | 内容 |
|------|------|
| 質問 | 新設ラベルとして追加する判断は妥当か。既存で代替できないか |
| 判断 | ✅ 許可 |
| 理由 | (1) 実コード確認: review-integrity/SKILL.md は前回レポートの発見を `data/shared/integrity-reviews/latest`（ディレクトリ＋symlink）で行っており、**ラベルに機能依存していない**。ラベルは報告 issue の一覧性のためだけで、削除しても機能は壊れないが、#114 が実際にラベル無し投稿を強いられた実害があり、定義追加が最小修正。(2) 既存代替の検討: 種類ラベル（docs/chore 等）への相乗りは「issue 層の作業種別」という既存の意味論を壊す。報告 issue は worktree を持たず個別マージもされないため、階層/種類/状態/終了のどこにも収まらない。「報告」節の新設は体系を増やすが、**誤った節に押し込むより一貫性が高い** |
| 参照 | review-integrity/SKILL.md:103-181、labels.md、setup-labels.sh |
| 自信度（参考記録） | 85% |
| 停止条件チェック | 該当なし |

### 2. `test` → `validation` 置換（review-integrity/SKILL.md:261）

| 項目 | 内容 |
|------|------|
| 質問 | 定義済み `validation` への置換は妥当か |
| 判断 | ✅ 許可 |
| 理由 | 実箇所は `--label "{bug|refactor|test}"`（発見からの修正 issue テンプレート）。テスト追加 issue は「実装の正しさの確認」であり、定義済みラベルでは `validation` が最も意味が近い。新設せず既存を使う判断は「既存優先」に合致 |
| 参照 | review-integrity/SKILL.md:261、labels.md 種類ラベル表 |
| 自信度（参考記録） | 80% |
| 停止条件チェック | 該当なし |

### 3. `subtask` ラベルの削除（issue-branch/SKILL.md:68）

| 項目 | 内容 |
|------|------|
| 質問 | 削除して issue-branch の機能が壊れないか |
| 判断 | ⚠️ 警告付き許可 |
| 理由 | 機能面: `subtask` をフィルタ・参照する箇所は全リポジトリで issue-branch:68 の1箇所のみ（grep で確認）。親子の追跡は本文の「親Issue: #N」とコメントで行われており、ラベル削除で issue-branch は壊れない。規約面: issue-hierarchy.md の階層表は「小タスク＝ラベル **—**」で、未定義ラベルの除去は規約に一致する。**警告**: 仕様の「小タスクには種類ラベル（feature 等）を付ける」は、同表の「—」および labels.md「種類ラベル（**issue 層**。必須）」と緊張関係にある。最小修正は `--label "subtask"` の**単純削除（ラベル無しで作成）**。種類ラベルを付ける設計にするなら issue-hierarchy.md の表と同時に整合させること（片側だけ直すと D2 と同型の乖離が再発する） |
| 参照 | issue-branch/SKILL.md:54-100、issue-hierarchy.md 階層表、labels.md |
| 自信度（参考記録） | 80% |
| 停止条件チェック | 該当なし |

### 4. `gap-fixed` ラベルの削除（out-of-date 除去＋コメントで代替）

| 項目 | 内容 |
|------|------|
| 質問 | 削除して issue-gaps の機能が壊れないか |
| 判断 | ✅ 許可 |
| 理由 | (1) 依存確認: `gap-fixed` の使用は issue-gaps/SKILL.md 内部のみ（22, 63, 97-107, 362）。他スキル・スクリプトに読み手が存在しないことを grep で確認済み。(2) 意味の代替可能性: SKILL.md:63 が「gap-fixed 付与（**元out-of-dateの場合**）」と限定しており、この状態変化は out-of-date の除去で完全に表現できる。解消内容のコメント（既存の Phase 3 コメント投稿）は仕様でも維持される。(3) 「同じ状態を2通りで表さない」は in-progress 廃止（labels.md ★節、157件中126件残留の実害）と同じ根拠で、既存パターンへの追従。**注意**: SKILL.md 冒頭 Actions 節(22行目)・Verdict 表(63行目)・Phase 5 レポート雛形(362行目)の3箇所も同時に更新し、104行目だけ直さないこと |
| 参照 | issue-gaps/SKILL.md 全体、labels.md「in-progress は使わない」 |
| 自信度（参考記録） | 90% |
| 停止条件チェック | 該当なし |

### 5. `qa-pending` の記述削除（issue-finish/SKILL.md:114）

| 項目 | 内容 |
|------|------|
| 質問 | 擬似コードの削除で問題ないか |
| 判断 | ✅ 許可 |
| 理由 | 実コード確認: 当該行はコメントアウトされた擬似コード（`# gh issue create ...`）で実行経路ではない。qa-ask/SKILL.md はラベルを一切使わないことを確認済みで、QA フローに読み手はいない。**注意**: 119行目の平文「ラベル: `qa-pending`」も同時に削除すること（V2 の文字列 grep が 0 件になる条件） |
| 参照 | issue-finish/SKILL.md:108-121、qa-ask/SKILL.md |
| 自信度（参考記録） | 90% |
| 停止条件チェック | 該当なし |

### 6. D2: labels.md の説明文をスクリプトに合わせる

| 項目 | 内容 |
|------|------|
| 判断 | ✅ 許可 |
| 理由 | setup-labels.sh ヘッダが自らを「ラベル定義の単一情報源」と宣言しており、GitHub 上に表示されるのはスクリプト側の文言。ドキュメントを実体に合わせる方向は Single Source of Truth（dev-guidelines.md）に合致 |
| 自信度（参考記録） | 90% |
| 停止条件チェック | 該当なし |

### 7. D3: setup-labels.sh の `-h|--help` と不明引数エラー

| 項目 | 内容 |
|------|------|
| 判断 | ✅ 許可 |
| 理由 | 現状 `--prune` 以外の全引数（--help 含む）がラベル作成にフォールスルーすることをスクリプトで確認。check-skill-references.sh の `case` パターンと同型で既存パターンへの追従。V5/V6 が「作成が走らない」「skip 経路（exit 2）の退行なし」を両方向で押さえている |
| 参照 | setup-labels.sh:66-70、check-skill-references.sh:36-40 |
| 自信度（参考記録） | 90% |
| 停止条件チェック | 該当なし |

### 8. D4: check-skill-labels.sh の除外条件は緩すぎないか

| 項目 | 内容 |
|------|------|
| 判断 | ⚠️ 警告付き許可 |
| 理由 | コメント行除外・`$...` 除外は静的検査の必然（`--label "$TYPE"` 等は検査不能）で、件数表示の義務（無言の切り捨て禁止）も #117 を踏襲しており妥当。**警告（緩い箇所を1点特定）**: `{...}` の一律除外は、まさに今回 `test` が隠れていた行 `--label "{bug|refactor|test}"` を検査対象外にする。修正後に同型の退行（`{bug|refactor|test}` の再混入）が起きても検出できない。**推奨**: `{...}` が純粋なラベル選択肢（`^\{[a-z|-]+\}$` 形）の場合は `|` で分割して各候補を LABELS と照合し、自由記述プレースホルダ（`{問題の要約}` 等）のみ除外する。実装コスト小で、今回の実失敗パターンを再発防止の網に入れられる |
| 参照 | review-integrity/SKILL.md:261、check-skill-references.sh、#117 |
| 自信度（参考記録） | 75% |
| 停止条件チェック | 該当なし |

### 9. V1〜V8 は十分か

| 項目 | 内容 |
|------|------|
| 判断 | ⚠️ 警告付き許可（3点の補強を推奨） |
| 理由 | 各項目は PASS/FAIL 判定可能で、双方向検証（V7）・退行防止（V6, V8）も入っており骨格は十分。補強推奨: (a) **V4 の「16件」は review-integrity 追加後 17件になる**。件数を固定で書くと追加自体との矛盾で必ず片方が FAIL するため「全件（追加後 17件）」に修正。(b) 除外の**件数表示**が実際に出ることの検証が V7 に含まれていない（仕様本文の要求なのに検証項目がない）→ V7 に (c) として追加推奨。(c) check-skill-labels.sh 自体の `-h|--help`／不明引数の挙動検証（D3 と同じ基準を新スクリプトにも適用） |
| 自信度（参考記録） | 80% |
| 停止条件チェック | 該当なし（S4 非該当: 全項目が判定可能。上記は精度向上の推奨） |

## 停止判断

該当なし。S1: 必読3ファイルは既定節が揃っており存在（プロジェクト固有節は未記入、上記メタ情報に記録）。S2: 親 task #115 の goal は確定済みで解釈は一意。S3: invariants / ADR への抵触なし（むしろ Single Source of Truth と in-progress 廃止の既存判断に沿う）。S4: 検証チェックリストあり、全項目判定可能。S5: 既存パターン（#116/#117、in-progress 廃止）への追従で、逸脱する設計はない。S6: experiment に非該当。

## goal 書き換えチェック

なし。仕様は #114 C3＋Medium/Low の解消と再発防止検査の追加であり、task #115 の goal をそのまま実現する内容。goal の範囲・成功条件の変更は含まれない。

## 要確認フラグ（停止には至らないが不確実性が残る項目）

- [ ] 判断3: 小タスクを「ラベル無し」にするか「種類ラベル付き」にするか。後者なら issue-hierarchy.md の階層表（—）を同時に更新すること
- [ ] 判断8: `{a|b|c}` 形式の選択肢は分割照合に格上げすることを推奨
- [ ] 判断9: V4 の件数（16→17）、除外件数表示の検証、check-skill-labels.sh の --help 検証
- [ ] .spec/ 3ファイルのプロジェクト固有節が未記入（既定のみで判断。確度低下の自覚として記録）
