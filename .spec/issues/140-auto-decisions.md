# Issue #140 自動判断ログ

## メタ情報
- 判断日時: 2026-08-03
- 自動処理: /task-run（親 task #139）
- 判断対象: `.spec/issues/140-pin-dependencies.md`（依存のバージョン固定）

## 必読コンテキストの状態

| ファイル | 状態 |
|---|---|
| `.spec/core-rules.md` | 既定節あり。**プロジェクト固有節は未記入** |
| `.spec/invariants.md` | 既定節あり。**プロジェクト固有節は未記入** |
| `.spec/known-issues.md` | 既定節あり。**プロジェクト固有節は未記入** |

→ S1 非該当（既定節が揃っている）。プロジェクト固有のルールが未登録のため、
既定のみで判断した。プロジェクト固有の失敗パターンは既定では捕捉できないことを自覚しておく。

## ゴール書き換えチェック（最優先）

親 task #139 の goal（成功条件1〜3が本 issue の担当。原文を受領済み）と仕様を照合した。

- 仕様は goal の文言を変更していない。成功条件3を原文どおり引用している
- 仕様が加えている解釈は2つ（下記）。いずれも**範囲・成功条件の変更ではなく解釈の明確化**と判定
  1. 「固定されている」＝ ビルドが特定の版に決定論的に解決され、**版を変える場所が対象ごとに1箇所**にある状態（既定値を持つ `ARG` を含む）
  2. 「一律固定していない」＝ 全対象を固定したら不合格（goal 自身が例として claude-code 非固定を挙げており、この読みは goal の例示と一致する）

→ 書き換えなし。解釈2件を本ログに明記した（エージェント定義の「解釈の明確化 → 警告付き許可」に該当）。

## 判断一覧

### 1. D1: `torch` を `ARG TORCH_VERSION`（既定 2.13.0）で固定し、値は派生プロジェクトが決める

| 項目 | 内容 |
|------|------|
| 質問 | goal 条件1「torch がバージョン固定されている」を満たすか。「既定値を置いただけで固定していない」＝ goal を小さく読んでいないか |
| 判断 | ⚠️ 警告付き許可（解釈の明確化を伴うため。解釈は上記ゴールチェック節に記録） |
| 理由 | (a) **「固定していない」との判定は当たらない。** 現状は `pip install torch`（完全無指定）であり、rebuild のたびに最新版へ動く。変更後は override しない限り `torch==2.13.0` に決定論的に解決され、ビルドごとの再現性が成立する。既定値付き ARG は「取れなければ最新」型の Fallback ではなく、**具体的な版の pin** である（Fallback ホワイトリスト分類でも「設定のデフォルト値」＝許可側。かつ存在しない版ではビルドが落ちる＝R-D01 適合、V9 で検証）。(b) **「値は派生側が決める」はテンプレートという成果物の性質から導かれた設計であり、goal の範囲縮小ではない。** goal 条件1は「固定されている」ことを求めており、誰が値を所有するかは規定していない。テンプレートが版を決め打つと全派生が同時に老朽化するという論点は goal の趣旨（再現性）と整合する。(c) `+cpu` ローカル版指定子を付けない判断は、インデックス URL と版指定の2箇所同時修正を要する状態（単一情報源違反）を避けるもので妥当。既定値 2.13.0 は #141 の実測値であり推測ではない |
| 警告 | 「固定」の解釈（既定値付き ARG で足りる）を本ログに残した。ユーザーがこの解釈を否とする場合（例: 派生プロジェクトに値の決定を強制する仕組みまで要求する場合）は差し戻すこと |
| 参照 | 親 goal 条件1・仕様「誰のための固定か」節、`.spec/core-rules.md` R-D01、Dockerfile:85-88 現物、`.spec/issues/141-non-root-user.md`（2.13.0 の実測出所） |
| 自信度（参考記録） | 85% ※停止判定には使用しない |
| 停止条件チェック | 該当なし |

### 2. D2: CPU ベースイメージをパッチまで固定（digest はやらない）

| 項目 | 内容 |
|------|------|
| 質問 | digest をやらない判断根拠が十分か |
| 判断 | ⚠️ 警告付き許可（実装条件1件を付す） |
| 理由 | (a) goal 条件2の文言は「**ベースイメージがパッチまで固定**」であり、パッチ固定は goal の文言と正確に一致する。digest は goal が求める水準を超える。(b) digest 固定を退ける根拠（同一パッチ版への Debian セキュリティ再ビルドまで止まる・CI 無し環境で sha256 手動貼り替えの運用コスト）は具体的で、トレードオフの両側が記録されている。(c) GPU 側 `24.12-py3` を「日付タグで実質固定済み」とする判断も妥当（latest 系の浮動タグではない） |
| 警告（実装条件） | **`python:3.11` は現物で2箇所に定義されている**: `Dockerfile:5` の `ARG BASE_IMAGE=python:3.11`（既定値）と `cpu/docker-compose.override.yml:9`（実際のビルド経路で使われる値）。さらに `docs/devcontainer-internals.md` にも記載がある。仕様 D2 は**どちらを単一情報源にするかを指定していない**。両方を `3.11.15` にすると同じ版が2箇所に出て V4 に自己矛盾する。実装時に単一情報源（推奨: compose override 側。Dockerfile 既定値は「compose を経ない直接 build 用のフォールバック」として役割を分け、コメントで明示）を決め、V4 の判定対象に BASE_IMAGE を含めること |
| 参照 | 親 goal 条件2、dev-guidelines「単一情報源の原則」、Dockerfile:5 / cpu override:9 現物 |
| 自信度（参考記録） | 80% ※停止判定には使用しない |
| 停止条件チェック | 該当なし（S5 非該当: 既存の ARG/override パターンに沿う） |

### 3. D3・D4・D5: 固定しない判断（claude-code / Node.js / feature）がサボりでないか

| 項目 | 内容 |
|------|------|
| 質問 | 「一律固定しない」の名を借りた手抜きになっていないか。goal 条件3は判断根拠の記録を求めているだけで、固定しない自由を無制限に与えてはいない |
| 判断 | ✅ 許可 |
| 理由 | 3件とも**対象固有の根拠**があり、かつ全体を貫く判別基準が一貫している（①実験の数値再現性に影響するか、②既に実質固定されているか、③CI 無し運用での更新コスト）。個別に検証した: **D3 claude-code** — goal 自身が非固定の例として claude-code を明示しており、goal の例示そのものへの適合。数値再現性に寄与しない開発ツールで、固定すると全派生が同時に古くなるという帰結も具体的。根拠を Dockerfile コメントに残す実装まで含む（V3）。**D4 Node.js** — `setup_20.x` は既にメジャー固定であり「無指定」ではない。claude-code の実行環境という位置づけと判別基準①への言及があり、判断としては D3 に従属する形で一貫。**D5 feature lock** — 単なる不作為ではなく、CPU/GPU 非対称・同一 digest の2箇所書き（単一情報源違反）・自動再生成による git 汚れ（#122 で実績のある偽還流候補の失敗パターン）という3つの積極的根拠がある。gitignore 追加という実装を伴い、判断根拠は本仕様に残る。**サボりとの識別点**: サボりなら「開発ツールだから」の一言で全対象を落とすはずだが、本仕様は torch（固定）と claude-code（非固定）を同じ基準①で逆方向に判定しており、基準が結論から独立して機能している |
| 軽微な指摘（停止不要） | D4・D5 の根拠の記録先が本仕様ファイルのみで、D3 のような Dockerfile 内コメントが無い。条件3の「記録されている」は仕様ファイルでも満たすが、Dockerfile を読む人（派生プロジェクト側）からは見えない。V3 を拡張して非固定対象すべての根拠コメントを確認するのが望ましい（追加検証項目 V13 として提案） |
| 参照 | 親 goal 条件3（原文引用: 「一律固定していない — 対象ごとに固定要否の判断根拠が記録されている（例: claude-code は頻繁更新なので固定しない）」）、known-issues の #122 偽還流問題への言及、Dockerfile:68/76 現物 |
| 自信度（参考記録） | 85% ※停止判定には使用しない |
| 停止条件チェック | 該当なし |

### 4. 検証チェックリスト V1〜V11 の十分性（特に V9）

| 項目 | 内容 |
|------|------|
| 質問 | 不足がないか。V9（negative control）の設計は妥当か |
| 判断 | ⚠️ 警告付き許可（追加項目3件を条件とする） |
| 理由 | **V9 は妥当かつ本質的。** 「存在しない版でビルドが落ちる」は、版指定が実際に効いていること（Fallback で握り潰されないこと）の直接検証であり、R-D01 / KI-D03（前提が崩れた入力を黙って受理する）を先回りで塞ぐ。「Fallback ホワイトリスト: なし」の宣言と対になっている。V8 の「完走しなければ未検証と明記。通ったと書かない」も R-D05 に正しく適合。全項目 PASS/FAIL を機械的に判定でき S4 非該当 |
| 追加すべき項目 | **V12**: CPU の実ビルド経路（compose override）で使われる BASE_IMAGE がパッチ固定値であり、`Dockerfile:5` の既定値が競合する第2の情報源になっていない（判断2の警告の検証。V2 は「固定されている」ことしか見ず、2箇所定義を検出しない）。**V13**: 固定しない対象（claude-code / Node.js / feature / uv）**すべて**について判断根拠が記録されている（V3 は claude-code のみ。goal 条件3は「対象ごとに」を要求している）。**V14**: GPU イメージの torch が同梱版のままである（`INSTALL_TORCH_CPU=false` が効いており pip の上書きが起きていない）。仕様の「想定される失敗」表が自らこの失敗モードを挙げているのに、検証項目が無い。V8 同様、エミュレーションで完走しなければ「未検証」と明記する |
| 参照 | `.spec/core-rules.md` R-D01・R-D05、`.spec/known-issues.md` KI-D03、仕様「想定される失敗と切り分け」表 |
| 自信度（参考記録） | 80% ※停止判定には使用しない |
| 停止条件チェック | 該当なし（チェックリストは存在し判定可能。不足は追加で解消） |

### 5. スコープ外項目が goal を小さく読んでいないか（S8 相当の確認）

| 項目 | 内容 |
|------|------|
| 質問 | apt パッケージ版 / uv / checksum 検証 / GPU ベースイメージをスコープ外とする判断が、goal の範囲縮小になっていないか |
| 判断 | ✅ 許可 |
| 理由 | S8 の操作的定義（親の完了条件から原文引用できるか）で逆向きに確認した: 親 goal 条件1〜3の原文に、apt パッケージ・uv・checksum の固定を**要求する文言は存在しない**。条件1は torch を名指し、条件2はベースイメージを名指し、条件3はむしろ非一律を要求する。したがってスコープ外化は goal が求める項目を落としていない。各項目に個別の根拠（apt はベースイメージのパッチ固定で覆われる／uv は基準①／checksum は #64 で見送り判断済み／GPU は日付タグで条件2を既に充足）が記録されており、条件3の要求様式にも適合。なお checksum 見送り（#64）は本 issue の外の既決事項をそのまま参照しており、本仕様が新たに範囲を狭めたものではない |
| 参照 | 親 goal 条件1〜3 原文、仕様「変更しないもの」表 |
| 自信度（参考記録） | 85% ※停止判定には使用しない |
| 停止条件チェック | 該当なし（S8: 落とされた項目はいずれも親の完了条件に無い。未着手の子 issue を落とす提案でもない） |

## 停止条件の総括

| 条件 | 判定 |
|---|---|
| S1 | 非該当（3ファイルとも既定節あり。プロジェクト固有節未記入は記録のみ） |
| S2 | 非該当（親 goal は確定・原文引用済み。解釈2件は本ログに明記） |
| S3 | 非該当（invariants / ADR への抵触なし。むしろ単一情報源の原則を仕様が積極適用） |
| S4 | 非該当（V1〜V11 は全項目判定可能。不足3件は追加で解消） |
| S5 | 非該当（ARG 外出し・compose override は既存 Dockerfile のパターンそのもの） |
| S6 | 非該当（negative を結論として扱う提案ではない。V9 は逆に negative control の追加） |
| S7 | 非該当（クローズ提案ではない） |
| S8 | 非該当（判断5参照） |

→ **停止せず前進。総合判定: 警告付き許可**（追加検証項目 V12〜V14 と D2 の単一情報源指定を条件とする）。

## 要確認フラグ（停止には至らないが不確実性が残る項目）

- [ ] BASE_IMAGE の単一情報源をどちらにするか（推奨: compose override。Dockerfile:5 の既定値は直接 build 用フォールバックとして役割をコメントで明示）— 実装時に確定し、V12 で検証
- [ ] `python:3.11.15` タグの実在は実装時の V6 で確認される（仕様の「実測」記載を信頼したが、本ログ作成時点で独立に pull 確認はしていない）
- [ ] `torch==2.13.0` の既定値も同様（#141 実測の転記を信頼。V7 で確認される）
- [ ] D4（Node.js）・uv の非固定根拠を Dockerfile コメントにも残すか（V13 の判定様式に依存）
