# Issue #75 自動判断ログ

## メタ情報

- 判断日時: 2026-07-30
- 自動処理: `/issue-auto 75`（完全自動モード）
- 対象 Issue: #75 `survey: エージェント運用ノウハウの文献サーベイ`（label: `survey`, `in-progress`）
- ブランチ / worktree: `survey/75-agentic-workflow-practices` / `worktrees/issue75`
- スナップショット: `pre-auto/20260730-102658`
- 判断者: auto-reviewer エージェント

### 参照コンテキストの代替について

`.claude/agents/auto-reviewer.md` が必読とする `constitution/core-rules.md` / `specs/invariants.md` / `specs/known-issues.md` は**本リポジトリに存在しない**（Issue #79 検出項目2として既に起票済み）。以下を正本として代替した。

| 代替した参照 | 実体 | 根拠となる条項 |
|---|---|---|
| `constitution/core-rules.md` | `.claude/CLAUDE.md` | 重要注意事項5「ルールは絶対」、6「省略・逸脱する前に確認」 |
| `specs/invariants.md` | `.claude/CLAUDE.md`「成果物の保存場所」「開発ガイドライン」 | 単一情報源の原則、対症療法的修正の禁止 |
| `specs/known-issues.md` | Issue #77 / #79、commit `4b16e2c`（project-specific contamination 除去） | 過去の失敗パターンの実記録 |

必読ファイルの不在を理由に全件を自信度 <50% とする機械的停止は行っていない。CLAUDE.md に明文の根拠がある項目は通常評価、根拠が無く**かつ決定が不可逆**な項目は非破壊側に倒した。

### 並行セッションの検出

判断中に、同一リポジトリで `/issue-auto 78` が並行稼働していることを確認した。

| 証拠 | 内容 |
|---|---|
| スナップショット | `pre-auto/20260730-102619`（本セッションの `102658` より 39 秒先行） |
| worktree | `worktrees/issue78`（branch `survey/78-derived-project-audit`） |
| 副産物 | Issue #79 が 2026-07-30T01:29:36Z に作成済み |

#78 は `Blocks: #70,#71,#72,#73,#74,#76` を宣言しており、**#70〜#74 の本文を書き換える予定**。D10 / D12 でフィードバックをコメントのみに限定した直接の根拠である。

---

## 判断一覧

### D1. 精読の深さ

| 項目 | 内容 |
|---|---|
| 質問 | (A) 全20本全文精読 / (B) 実在性＋abstract全件検証＋重点8本のみ全文 / (C) 8本のみ着手 |
| 判断 | ✅ **(B) を許可** |
| 理由 | (C) は Issue 完了条件「各文献の主張が一次資料で検証されている」を満たせないため却下。(A) は約435kトークンで、本サーベイ対象の 2605.12366（コンテキスト長に伴う見落ち増大）が警告する状況を自ら招く自己矛盾。(B) が完了条件を満たすのは「abstract も一次資料である」ため成立するが、成立条件は厳格で、**abstract から読み取れない主張を採録しないこと**が必須。 |
| 参照 | Issue #75 完了条件2、Issue #75 D節、実現性レビュー、CLAUDE.md「対症療法的修正の禁止」 |
| 自信度 | **85%** |

**検証範囲の語彙（必須）**: `全文 v{N} §{節}` / `abstract v{N}` / `メタデータのみ` / `検証不能`。
`abstract v{N}` の行に手法詳細・数値・比率を書くことは禁止。

### D2. 成果物のファイル形態

| 項目 | 内容 |
|---|---|
| 質問 | (a) 本編＋文献目録の2ファイル / (b) 単一ファイル |
| 判断 | ✅ **(b) 単一ファイル** `docs/surveys/2026-07-30_agentic-workflow-practices.md` |
| 理由 | CLAUDE.md「成果物の保存場所」はフラット1ファイルを図示して規定しており、分割は明文規定からの逸脱にあたる。分割を推奨する「300行基準」は CLAUDE.md に存在しない review-spec 内部基準であり明文規約に優先しない。既存 `docs/devcontainer-internals.md` が243行で運用されており300行は実効上限でない。 |
| 参照 | CLAUDE.md「成果物の保存場所」、重要注意事項5・6、実測（`docs/*.md` = 89 / 207 / 243行） |
| 自信度 | **85%** |

**逸脱の予防条件**: 600行超の見込みになった時点で停止しユーザーに分割可否を確認する。無断分割も無制限な肥大化も禁止。
**書式**: `docs/security.md` に倣う（サマリー表 → 固定サブ構造の詳細節 → 総合評価、H3まで、脚注なし、インラインリンクのみ、YAML front-matter なし）。

### D3. 知見の SSOT

| 項目 | 内容 |
|---|---|
| 質問 | 事実の正本＝サーベイ文書、設計判断の正本＝各Issue、コメントはアンカー参照＋Issue固有の解釈のみ |
| 判断 | ✅ **許可**（無条件） |
| 理由 | CLAUDE.md「単一情報源の原則」の直接適用。文献の主張を6箇所に転記すれば1本の誤り訂正に6箇所修正が必要。実測で既に記述誤り3件が確認済み＝訂正は実際に発生する前提。 |
| 参照 | CLAUDE.md「単一情報源の原則」、実測（記述誤り3件） |
| 自信度 | **92%** |

**アンカー形式**: arXiv ID 由来の安定形（`#ref-2601-03315`）。見出し文言由来のアンカーは使わない。

### D4. Issue #75 本文の文献リストの扱い

| 項目 | 内容 |
|---|---|
| 質問 | (a) 凍結＋注記1行追記 / (b) 誤りを本文で直接修正 / (c) 放置 |
| 判断 | ✅ **(a)** |
| 理由 | (c) は誤った件数等が参照され続けるため不可。(b) は SSOT 違反であり、かつ**初期スクリーニングで何を誤ったかの記録自体が本Issueの成果物**。Issue #78 は誤りを本文に明記して方法論的教訓として保全する書き方を既に採っており同一リポジトリ内の先例。誤りの上書き消去は、症状を消して原因（Web検索要約への依拠）の記録を失う対症療法。 |
| 参照 | Issue #78 本文の誤り保全記述、CLAUDE.md「単一情報源の原則」「対症療法的修正の禁止」 |
| 自信度 | **85%** |

**レビュー結果への訂正**: review-spec は「2605.12366 の条件欠落」を指摘したが、実測の結果**条件欠落は Issue #75 本文 D節のみ**で、**Issue #74 本文には「800K トークン以降で 2〜30 倍」と条件が既に明記されている**。訂正対象を #74 に及ばせないこと。

### D5. 検証ステータス列の粒度

| 項目 | 内容 |
|---|---|
| 質問 | 1列 / 2列（存在検証・主張検証を分離） |
| 判断 | ✅ **2列**（`存在検証` / `主張検証`）＋ `検証範囲` の計3列 |
| 理由 | Issue 注意書きが「arXiv ID **や** 主張内容」と2つを並置。実測で両者が明確に解離した（ID は 20/20 実在・タイトル一致だが主張レベルで誤り3件）。1列に畳むと「ID が実在するから検証済」という誤った安心を生む。 |
| 参照 | Issue #75「## 注意」、実測 |
| 自信度 | **90%** |

### D6. 査読ステータスの表現

| 項目 | 内容 |
|---|---|
| 質問 | 2値 / 3層 / 5段階 |
| 判断 | ⚠️ **5段階を警告付きで許可**。ただし第⑤層の呼称を「純プレプリント」から「**査読情報未確認（arXiv メタデータ上）**」に改める |
| 理由 | 2値では SANER 2026 Stage 1 Registered Report をプレプリントと同列に落とし、逆に Agents4Science 2025（AI筆頭著者必須の非標準会場）を「査読あり」に昇格させてしまう。実測で5層すべてに実データが入る（2/2/1/1/14）。<br>**警告**: `comments`/`journal-ref`/`DOI` 欄が空であることは査読を経ていないことの証明ではない（未記入・投稿中の可能性）。証拠のない断定は「未検証に検証済と記録」と同種の誤り。 |
| 参照 | Issue #75 作業項目3、実測（5層分布 2/2/1/1/14）、auto-reviewer.md 注意事項2「楽観的にならない」 |
| 自信度 | **82%** |

### D7. スコープ拡大 — 新カテゴリF（引用ハルシネーション文献）

| 項目 | 内容 |
|---|---|
| 質問 | `2604.03173` / `2604.03159` / `2602.05930` / Retraction Watch 分析 を追加するか |
| 判断 | ⚠️ **警告付き許可。上限6件**（arXiv 3本＋二次情報 最大3件） |
| 理由 | Issue 作業項目2が「検索の追補（芋づる探索が未実施）」を**明示的に指示**しており、当初リストに無いことはスコープ外の根拠にならない。この群は**本サーベイ自身の検証手法の妥当性根拠**であり、カテゴリFを入れないと本サーベイは自分の方法論的弱点を無根拠に主張することになる。 |
| 参照 | Issue #75 作業項目2・「## 注意」、実測（記述誤り3件、偽陰性2件） |
| 自信度 | **80%** |

**制約**: ①Retraction Watch / Lancet 系は二次情報として明示。②`2602.05930` が D6 第①層の前提を弱める含意を明記（無視は確証バイアス）。③カテゴリFは「検証手法の妥当性」節に限定し点1〜5の設計根拠には使わない。

### D8. 「検索の追補」の停止条件

| 項目 | 内容 |
|---|---|
| 判断 | ⚠️ **警告付きで規定**（Issue に明文根拠がなく auto-reviewer が代理設定） |
| 理由 | 停止条件なしで実行すると無限探索になり、本サーベイが調査対象とする長ホライズンタスクの失敗（2604.11978）を再現する。 |
| 自信度 | **72%** → 要確認 |

| 項目 | 値 |
|---|---|
| 追補の総上限 | **8件**（カテゴリF最大6件＋点1〜5直結の追補最大2件） |
| 探索の深さ | **1ホップまで** |
| 採録の必要条件 | 「点1〜5 のどれ／完了条件のどれに直結するか」を1行で書けること |
| 早期打ち切り | marginal contribution ゼロが3件連続 |
| 総文献数の上限 | **29本**。超える場合は停止しユーザー確認 |

上限で漏れた候補は削除せず「未調査の候補（次のサーベイへの申し送り）」として列挙する。

### D9. agentsstandard.com の扱い

| 項目 | 内容 |
|---|---|
| 判断 | ✅ **(a) 訂正＋格下げし、業界標準の出典を `agents.md`(Linux Foundation/AAIF) に差し替え** |
| 理由 | (b) は確定した記述誤りを残すため不可（実仕様は nearest-file-wins ではなく階層 concatenate）。(c) も不可：Issue が明示的に列挙した文献を無言で消すことは初期スクリーニングの記録を失わせ、「取得失敗行の削除・不言及」の禁止と同じ問題。(a) が唯一、誤りを訂正しつつ記録を保つ。出典の差し替えは症状（誤った1文）の書き換えではなく原因（誤った出典への依拠）の修正。 |
| 参照 | Issue #75 D節・作業項目3、実測、CLAUDE.md「対症療法的修正の禁止」 |
| 自信度 | **88%** |

### D10. 出典不明の3主張（#72 / #73 / #74 本文）

| 項目 | 内容 |
|---|---|
| 判断 | ✅ **(a) 出典特定を試み、結果をサーベイ文書に明記。各Issueへコメントで通知。本文編集は行わない** |
| 理由 | (c) 不可：#74 の「1.5K→6K トークンと4倍」は無出典の定量値が設計根拠に残る状態で、#78 が指摘した「観測なき推論」と同型。(b) 不可：**他Issue本文からの記述削除は不可逆**で、CLAUDE.md にも Issue #75 にも根拠がない。加えて #78 が同じ本文の書き換えを予定しており作業衝突で一方の編集が失われる。 |
| 参照 | 実測（#72 本文48行目 / #73 本文62行目 / #74 本文55行目に出典なし、#78 本文90行目 `Blocks:`）、Issue #75 完了条件3、CLAUDE.md 重要注意事項6 |
| 自信度 | **87%** |

優先確認先: #72 → `2605.15425`、#73 → `2601.03315` または `2604.02485`。
#74 の「4倍」が特定できない場合「この定量値を設計根拠にしないこと」を明示勧告する（本文は編集しない）。

### D11. #78 とのフィードバック矛盾時の優先順位

| 項目 | 内容 |
|---|---|
| 判断 | ⚠️ **(c) 都度判断を、明文プロトコル付きで警告付き許可**（無条件の「都度判断」は禁止） |
| 理由 | (b) 却下：#78 が「テンプレートのソースを読んだ推論に基づき実際の派生プロジェクトを一切観測していなかった。方法論として誤り」と明記し実際に誤結論を生んだ。一般文献が実測を上書きする設計は同じ誤りの反復。(a) の単純採用も却下：実測は「現状こうなっている」の記述であり「こうすべき」の規範ではない。実測優先を無条件化すると現状追認バイアスになり、本サーベイ C 節の主題（確証バイアス）に自己矛盾。 |
| 参照 | Issue #78 本文「## 背景」、Issue #75 C節（2604.02485）、auto-reviewer.md 自信度基準 |
| 自信度 | **70%** → 要確認 |

| 問いの種類 | 優先 |
|---|---|
| 現に何が起きているか（事実記述） | **#78（実測）優先**。文献で上書きしない |
| なぜそうなるか／どんな失敗モードか（機構・因果） | **#75（文献）優先** |
| 何をすべきか（設計判断） | 両方を併記して当該Issueに提示。決定は各Issueに委ねる |
| 同一命題で正面から矛盾 | 「未解決の矛盾」として両方を記録。消去は禁止 |

### D12. #70〜#74 へのフィードバック方法

| 項目 | 内容 |
|---|---|
| 判断 | ✅ **(a) コメント投稿のみ。(b) 本文編集は ❌ 禁止** |
| 理由 | ①**不可逆**：他Issue本文からの既存記述の除去は取り返しがつかず、権限を認める根拠がない。②**作業衝突**：#78 が同じ本文の書き換えを予定し `worktrees/issue78` が並行稼働中。③#70〜#74 は全て `blocked` で #78 待ちの状態にあり、ブロック中の本文を先に書き換えるのは依存順序に反する。CLAUDE.md「進捗報告のルール」はコメントを標準経路と規定。 |
| 参照 | 実測（#78 `Blocks:`、`worktrees/issue78` 存在、#70〜#74 に `blocked`）、CLAUDE.md「進捗報告のルール」 |
| 自信度 | **90%** |

### D13. 非実在判定の要件

| 項目 | 内容 |
|---|---|
| 判断 | ✅ **許可**（強く支持）。ただし F1 の原因は要件で覆うのではなく**根治**する |
| 理由 | 実測で偽陰性が2種発生。F1: `curl -L` 欠落で arXiv API が301を返し、実在確実な対照 `1706.03762` まで NOT_FOUND になった。F2: `2606.04967` は API で3回連続 `totalResults=0` だが abs ページは200でタイトル一致。単一チャネルの否定を非実在と断定していれば実在論文を捏造と誤認し虚偽報告を出していた。 |
| 参照 | 実測（F1 / F2）、auto-reviewer.md 注意事項2、CLAUDE.md「対症療法的修正の禁止」 |
| 自信度 | **92%** |

| 条件 | 判定 |
|---|---|
| いずれか1チャネルで肯定 | `実在` |
| 独立2チャネルがともに否定 **かつ** 同一実行内で陽性対照が成功 | `非実在の可能性あり`（断定語は使わない） |
| それ以外（単一チャネルのみ否定 / 陽性対照失敗 / タイムアウト） | **`検証不能`**（非実在と書かない） |

**F1 の根治**: 301 を「不在」と解釈する回避策ではなく取得コマンド側を修正する（`curl -L` を常用）。陽性対照 `1706.03762` を検証ループ先頭で必ず走らせ、失敗したら全結果を破棄・再実行する。

### D14. Fallback ホワイトリスト承認

| 項目 | 内容 |
|---|---|
| 判断 | ✅ **①〜⑦すべて許可**（③④⑤⑥に条件付与）。禁止事項リストは全項目を確定 |
| 参照 | `.claude/skills/review-spec/SKILL.md` 263-275行、auto-reviewer.md「Fallback 分岐」表 |
| 自信度 | **88%** |

| # | パターン | 判断 | 条件 | 分類 |
|---|---|---|---|---|
| ① | 検証ステータス列の初期値 = `未検証` | ✅ | 悲観側デフォルト。空欄禁止 | 設定のデフォルト値 |
| ② | 査読状態が確認不能なら `不明（未確認）` | ✅ | D6 第⑤層と混同しない（②はメタデータ取得自体の失敗、⑤は取得成功かつ査読情報の記載なし） | UI プレースホルダ |
| ③ | WebFetch のタイムアウト／リトライ回数のデフォルト | ✅ | リトライは最大3回。**枯渇時は必ず `検証不能` を記録**。無記録は「エラーの握りつぶし」に転落 | 設定のデフォルト値 |
| ④ | 取得済み一次資料のローカルキャッシュ再利用 | ✅ | **取得日時と到達 version (`vN`) を必ず記録**。記録のないキャッシュは再利用不可 | キャッシュミス |
| ⑤ | 長文引用の要約掲載 | ✅ | **鍵括弧引用にはロケータ必須**（`abstract` / `§n` / `vN`）。ロケータなしの逐語引用は禁止 | 表示の整形 |
| ⑥ | 同一 arXiv ID 内での取得経路切替（html→abs→pdf） | ✅ | **同一 ID 内に限る**。到達 version を記録 | 経路フォールバック |
| ⑦ | 部分完了を「完了条件未達」と明記して Issue 報告 | ✅ | 無条件許可。むしろ必須 | 正直な部分報告 |

**確定した禁止事項（すべて ❌）**

| 禁止項目 | 補足 |
|---|---|
| arXiv ID の黙った差し替え | ⑥の同一ID内経路切替とは明確に区別 |
| 一次資料より Issue メモを優先 | 実測で Issue メモ側に誤り3件が確定 |
| 取得失敗行の削除・不言及 | `検証不能` として残す |
| 未検証に `検証済` と記録 | ①の悲観デフォルトで機械的に防ぐ |
| abstract のみで手法・数値依存の主張を採録 | `検証範囲` 列で機械的に検出可能にする |
| 無関係な文献を無理に関連づけ | D8 の「1行で書けること」で防ぐ |
| 節番号・ページ番号の捏造 | ⑤のロケータ必須と対になる |
| モデルの事前知識で fetch せず記述 | カテゴリF（2604.03173）が示す最大のリスク |
| 完了条件チェックボックスの実態乖離 | ⑦と対。全チェック時のみ完了扱い |

### D15. `.spec/issues/` vs `.claude/spec/issues/` のパス不整合

| 項目 | 内容 |
|---|---|
| 判断 | ✅ **`.spec/issues/75-*.md` に書く。不整合修正はスコープ外。新規Issue化は ❌ 不要（#79 がカバー済み）** |
| 理由 | 実測で `.spec/issues` 参照が16箇所（8ファイル）、`.claude/spec/issues` 参照が0箇所。少数派に書けばスキルから読めなくなる。重ねて起票すれば CLAUDE.md「単一情報源の原則」に反し `duplicate` を作る。 |
| 参照 | 実測、Issue #79 検出項目3、CLAUDE.md「単一情報源の原則」 |
| 自信度 | **90%** |

**⚠️ 付随して検出した汚染リスク（自信度 78% → 要確認）**: `.spec/` は `.gitignore` 未登録のためコミット対象になり、テンプレート本体に Issue 固有の開発ファイルが残る。直近コミット `4b16e2c`「fix: remove project-specific contamination」で除去した種類の問題を再生産し、Issue #63 / #74 の論点にも抵触する。**今回のコミット対象から `.spec/` を除外し、恒久的な扱いは #79 に委ねる**。

---

## 停止判断

**該当なし。処理を継続する。**

- 自信度 <50% の項目はゼロ。全15項目について CLAUDE.md、Issue #75 本文、または実測事実に基づく根拠を確保した。
- 不可逆な操作（他Issue本文の書き換え = D10(b)/D12(b)、Issue #75 本文の誤記述の上書き = D4(b)、文献の無言削除 = D9(c)）は**すべて非破壊側の選択肢を採ることで回避**した。
- CLAUDE.md 規約からの逸脱を要する選択肢（D2(a) の複数ファイル分割）は**規約準拠側を選択**したため、重要注意事項6のユーザー確認は発生しない。

**自動処理の中断トリガ（以下に触れたら停止してユーザー確認）**

1. サーベイ文書が **600行を超える見込み**になった（D2）
2. 総文献数が **29本を超える**必要が生じた（D8）
3. 陽性対照（`1706.03762`）が失敗し検証パイプラインの健全性が確認できない（D13）
4. #78 の成果と**同一命題で正面から矛盾**し、かつ併記では設計判断が下せない（D11）
5. 他Issue本文の編集が必要だと判断される状況が生じた（D10/D12。この場合は編集せず必ず停止）

---

## 要確認フラグ（自信度 50-79%）

- [ ] **D11（70%）**: #78 との矛盾時の優先順位。CLAUDE.md に明文規定がなく、代理設定したプロトコル（事実記述 → #78 / 機構説明 → #75 / 設計判断 → 併記 / 正面矛盾 → 未解決として記録）の妥当性をユーザーに確認したい。
- [ ] **D8（72%）**: 追補の上限（8件・深さ1ホップ・総数29本上限・marginal contribution ゼロ3連続で打ち切り）は Issue に根拠がなく代理設定した数値。特に「総数29本」の規模感。
- [ ] **D15 付随（78%）**: `.spec/` が `.gitignore` 未登録のため判断ログがコミット対象になる。今回はコミット対象から除外する方針でよいか。
- [ ] **D7（80%、境界）**: カテゴリF追加によるスコープ拡大。`2602.05930` は D6 第①層の信頼度前提を弱めるためサーベイの結論構造に影響する。上限6件で妥当か。

---

## 実装フェーズへの指示

### 成果物と配置

1. 成果物は**単一ファイル** `docs/surveys/2026-07-30_agentic-workflow-practices.md`。分割は禁止（600行超見込みで停止しユーザー確認）。
2. 書式は `docs/security.md` に倣う：サマリー表 → 固定サブ構造の詳細節 → 総合評価。H3まで、脚注なし、インラインリンクのみ、YAML front-matter なし。
3. 判断ログは `.spec/issues/75-auto-decisions.md`。**コミット対象からは除外**する。パス不整合の修正は本Issueで行わない（#79 が担当）。

### 文献の検証

4. 精読方針は **(B)**：全21本の実在性＋abstract を検証し、点1〜5 に直結するものだけ全文精読。
5. 文献表に **3列**：`存在検証` / `主張検証` / `検証範囲`。`検証範囲` の語彙は D1 の4値のみ。
6. `abstract v{N}` の行に手法詳細・数値・比率を書かない。
7. 検証ステータスの初期値は `未検証`。空欄を作らない。
8. **非実在判定**は D13 の規則に従う。肯定は1チャネルで足り、否定は2チャネル一致＋陽性対照成功が必要。
9. `curl` は**常に `-L`**。検証ループ先頭で陽性対照 `1706.03762` を必ず走らせる。
10. 鍵括弧の逐語引用には**必ずロケータ**。書けない引用は掲載しない。
11. arXiv ID を黙って差し替えない。経路切替は**同一 ID 内**に限る。
12. リトライは最大3回。枯渇したら `検証不能` を記録。行の削除・不言及は禁止。

### 分類と表現

13. **査読ステータスは5層**。第⑤層の呼称は「**査読情報未確認（arXiv メタデータ上）**」。定義と判定根拠は凡例1箇所にのみ記述。
14. 「70%(14/20) が査読情報未確認」の分布を明示し、格下げ対象が AGENTS.md 標準だけではない事実を作業項目3への回答として書く。
15. Issue が「最優先」とする 2601.03315 は**非標準会場・著者2名・n=4 の事例研究**で証拠の強さは最弱の類型であることを明記。優先度指定に従って読むが証拠の強さを水増ししない。

### 訂正すべき記述（実測で確定した3件）

16. **agentsstandard.com**: 「二次情報／コミュニティ提案（個人リポジトリ `nbiish/` 由来、標準化団体・バージョン・日付の記載なし）」に格下げ。実仕様は nearest-file-wins ではなく**全 tier を global→project base→project active→folder の順に concatenate**（矛盾時のみ most specific が勝つ）。業界標準の出典は **`agents.md`（Linux Foundation 傘下 AAIF 管理）** に差し替え。完全除外はしない。
17. **2601.03315 の鍵括弧引用**は abstract に存在しない。全文で確認できなければ**引用として掲載しない**。6失敗モード/4設計原則は `abstract v{N}` として掲載可。
18. **2605.12366**: 「**800K トークン以降の benign activity の後**に見落ちが2〜30倍」と条件を必ず付ける。⚠️ **Issue #74 本文には既に 800K 条件が正しく記載されている。#74 は触らない。**
19. **タイトル相違5件**を正式名に統一。
20. **文献数は21本**（arXiv 20 ＋ Web 1）。arXiv ID は 20/20 実在・タイトル一致 → Issue が警戒した「ID 不正確」は **ID 水準では空振りだった**事実を明記する（誤りは主張水準に集中）。

### スコープ

21. **カテゴリF を追加**。上限6件。`2604.03173` / `2604.03159` / `2602.05930` を優先。Retraction Watch / Lancet 系は二次情報と明示。
22. カテゴリFは「本サーベイの検証手法の妥当性」節に限定し点1〜5の設計根拠には使わない。
23. `2602.05930` が **D6 第①層の信頼度前提を弱める**含意を、都合が悪くても明記する。
24. **追補の上限：合計8件**、深さ1ホップ、総文献数29本上限。
25. 上限で漏れた候補は削除せず末尾に「未調査の候補」として列挙。

### SSOT とフィードバック

26. **事実の正本＝サーベイ文書、設計判断の正本＝各Issue**。
27. アンカーは arXiv ID 由来の安定形（`#ref-2601-03315`）。
28. **#70〜#74 へのフィードバックはコメント投稿のみ**。本文編集は一切行わない。
29. **出典不明の3主張**: 出典特定を試み結果をサーベイ文書に明記。各Issueへコメントで通知。本文からの削除は禁止。
30. **#78 との矛盾**: D11 のプロトコルに従う。
31. Issue #75 本文の文献リストは**凍結**。注記1行のみ追記。
32. Issue #75 本文の `## 関係` に `Related: #78` を追記。

### 完了報告

33. 品質チェックは `scripts/quality-check.sh` を実行せず（`EXIT=2` を実測確認）、survey 向け代替チェック（リンク到達性、検証ステータス列の空欄なし、逐語引用のロケータ有無、完了条件の実態一致）を実施。**スクリプト自体は変更しない**（#79 が担当）。
34. 完了条件チェックボックスは**実態と一致させる**。部分完了は「完了条件未達」と明記して報告。
35. コミットは指示があるまで行わない（CLAUDE.md 重要注意事項4）。本セッションは `/issue-auto` の完全自動モードで実行されており、ユーザーから当該指示を得ている。
